* crear coleccion de usuarios con opciones para  que les avise por mail si quieren updates de las peliculas nuevas.
* limpio la tabla de updateadas cuando termino de hacer el crawling.
* todas las peliculas que levanto las guardo en una base de datos.
* cuando agrego una pelicula, prefunto si ya existia. si existia y y la nueva que agrego no es screener la updatea, si no no. cuando la updatea guarda las updateadas en una coleccion nueva.
* para cada pelicula que guardo, guardo tambien un hash de 8 digitos. para sharear y asdf
* cuando se updatea la collecion de updateadas mando mails a los usuarios que quieran


*** front end ***

* cuando el carousel mueve un elemento actualiza el hash en la url
* cuando entra a la pagina y tiene un hash, node tiene que renderizarlo bien
